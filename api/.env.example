KYC_PORT=5050
PUBLIC_API_BASE=

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ecodrive_db
ADMIN_LOGIN_ID=
ADMIN_PASSWORD=

# SMTP (choose one mode, then fill active values below)
# Mode A - Mailtrap Sandbox (testing only):
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USER=<mailtrap-sandbox-username>
# SMTP_PASS=<mailtrap-sandbox-password>
# SMTP_FROM=Ecodrive <no-reply@ecodrive.local>
# SMTP_SECURE=false
#
# Mode B - Mailtrap Live (requires verified domain):
# SMTP_HOST=live.smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USER=api
# SMTP_PASS=<mailtrap-live-stream-password>
# SMTP_FROM=Ecodrive <no-reply@mail.yourdomain.com>
# SMTP_SECURE=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Ecodrive <no-reply@ecodrive.local>
SMTP_SECURE=false

# Recommended SMS setup (built-in Semaphore relay, no separate SMS server)
SEMAPHORE_API_KEY=
SEMAPHORE_SENDERNAME=
SEMAPHORE_API_BASE=https://api.semaphore.co/api/v4
SEMAPHORE_REQUEST_TIMEOUT_MS=15000
SEMAPHORE_RELAY_TOKEN=

# Legacy/optional custom SMS webhook fallback
SMS_WEBHOOK_URL=
SMS_WEBHOOK_TOKEN=

AUTH_SESSION_TTL_MS=86400000
ALLOW_DEMO_OTP=false
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5500,http://localhost:5500
RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX=10
OTP_SEND_RATE_LIMIT_MAX=6
OTP_VERIFY_RATE_LIMIT_MAX=10
